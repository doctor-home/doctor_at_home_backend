Use maven plugin to build

-> 1. docker run mongo:4

-> 2. Navigate to doctor_at_home


-> 3. Run mvnw dockerfile:build  #faster option
	or Run mvnw clean package dockerfile:build 

-> 4. docker volume create --name=dah-repo

-> 5. docker run --name=dah-server --publish=80:8080 \
		--volume=dah-repo:/var/lib/athome/dah-repo -d \
     	doctor_at_home/athome:0.0.1-SNAPSHOT


